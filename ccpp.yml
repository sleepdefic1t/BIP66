name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: build
      run: mkdir build && cd build
    - name: cmake
      run: cmake .. && cmake --build .
    - name: tests
      run: ./test/bip66_tests
